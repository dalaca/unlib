New Item
<br/>

<%= form_for @item do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br/>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<br/>
	<%= f.label :note %>
	<%= f.text_field :note %>
	<br/>
	<%= f.submit "Create Item" %>
<% end %>